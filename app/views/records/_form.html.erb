<h3>お店の情報を入力</h3>
<%= form_with(model: record, local: true) do |f|%>
<%# 店名 %>
  <div class="field">
    <span class="required">必須</span>
    <%= f.label :store_name, "店名" %><br />
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%#= f.collection_select(:hoge, "#", :id, :name, {}, class:"select-box") %> 
  </div>

  <div class="field">
    <%= f.label :concept, "コンセプト" %><br />
    <%= f.text_area :concept, class: :form__text %>
  </div>

  <div class="field">
    <%= f.label :image, "写真" %><br />
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit "保存する", class: :form__btn  %>
  </div>
<% end %>